{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentic://schemas/v1/result-payloads/risk-signal-review",
  "x-result-schema": "result.risk-signal-review",
  "x-allowed-roles": ["Analyst"],
  "type": "object",
  "required": ["reviewed_candidates", "outcomes"],
  "properties": {
    "reviewed_candidates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["fingerprint", "status", "reason", "basis_refs"],
        "properties": {
          "fingerprint": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$"
          },
          "status": {
            "type": "string",
            "enum": ["confirmed", "not-applicable"]
          },
          "reason": {"type": "string", "minLength": 1},
          "basis_refs": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          }
        },
        "additionalProperties": false
      }
    },
    "outcomes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "instance_key",
          "definition_digest",
          "status",
          "summary",
          "basis_refs"
        ],
        "properties": {
          "instance_key": {"type": "string", "minLength": 1},
          "definition_digest": {
            "type": "string",
            "pattern": "^sha256:[0-9a-f]{64}$"
          },
          "status": {
            "type": "string",
            "enum": ["satisfied", "unsatisfied", "inconclusive"]
          },
          "summary": {"type": "string", "minLength": 1},
          "basis_refs": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "minLength": 1},
            "uniqueItems": true
          },
          "input_refs": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "pattern": "^sha256:[0-9a-f]{64}$"
            }
          },
          "freshness_refs": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "pattern": "^sha256:[0-9a-f]{64}$"
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
