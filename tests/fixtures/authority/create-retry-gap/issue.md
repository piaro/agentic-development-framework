# Add mutation audit records

Record an audit entry when a resource is created, updated, or deleted. The audit entry must identify the operation, actor, and result.

This requirement does not define a request key, new required input, retention claim, retry error response, or deletion behavior for such a claim.
