schema_version: 1
change: resource-audit
phase: pre-implementation
result: blocked
findings:
  - id: GOV-AUTH-001
    type: authority-missing
    decision: CD-CREATE-RETRY
    severity: blocking
    counterexample: The finding proves a gap but does not select the request-key product semantics.
required_action: Open a Decision Request and keep Contract Readiness blocked.
